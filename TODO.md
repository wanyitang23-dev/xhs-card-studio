# 待办

给接手的人（包括新开上下文的 agent）看的。每条都写清楚**为什么要做**、**怎么判定做对了**，
不需要读历史对话就能开工。

---

## 1. 渲染自动检查（优先级最高）

### 为什么

这个工具的产出是**视觉成品**，而它最常见的缺陷有一个共同点：
**读代码完全看不出来，必须渲染出来量才知道。**

已经发生过五次，每次都是用户肉眼发现 → 截图 → 人工用无头浏览器量 → 修：

| 缺陷 | 代码里看着 | 实测结果 | 提交 |
|---|---|---|---|
| 文字重叠 | 各块坐标都写得好好的 | 10 张卡有 6 张重叠，共 8 处，其中 4 处正文压在页脚上 | `8accd5b` |
| 白字掉出深色块 | 深色块 668px、标题 104px，都合理 | 标题实际到 714px，46px 白字 `rgb(253,250,245)` 落在 `rgb(245,241,234)` 上，对比度 **1.03:1** | `510a2d5` |
| 浮层被盖住 | 位置计算完全正确 | `.ledge` 在 537–679，`.slab` 0–637，命中测试返回 `.slab`，只露出 42px | `4bad4fd` |
| 装饰球掉进布局 | 一行普通 CSS（`.slab > *{position:relative}`） | 深色块 876 → **1708px**（卡片只有 1440），正文/格子/页脚分别超出底部 384/594/654px | `3d9523b` |
| 配图被裁 | `object-fit:cover`，很常见的写法 | 1170×2532 竖图塞进 928×300，比例从 0.462 变成 3.093 | `84931c8` |

**核心理由**：prompt 规则是「请求」，agent 可能听也可能不听；测量是「事实」。
凡是能确定性判定的，就不该靠 prompt 猜。（这也是 `6f9e13d` 砍规则时的结论。）

### 做什么

生成完成后，在**已经渲染在页面上的那个 iframe**里跑一遍测量，把问题标在界面上。
不调 agent、不花额度、毫秒级。

第一期做这三条（判据最硬、误报率最低，覆盖上表五个案例中的四个）：

1. **内容被裁掉**
   卡片是写死的 `width:1080px; height:1440px; overflow:hidden`。
   判据：任何子元素的 `getBoundingClientRect().bottom` 超过卡片底边 → 被裁。
   报告：`第 N 页：654px 内容被裁掉在卡片外`。

2. **文字重叠**
   判据：两个含文字的块，矩形在 x 和 y 两个方向都相交超过阈值（建议 4px，
   避免边框/阴影造成的误报）。
   注意只比较**直接承载文字**的元素，不要比较祖先容器（否则父子必然相交）。

3. **内容被遮挡**
   判据：对元素自己的中心点做 `document.elementFromPoint`，返回的不是它自己
   （也不是它的后代）→ 被别的东西盖住了。
   这条能抓住「布局正确但绘制顺序错」，是 `4bad4fd` 那一类。

第二期（判据更软，容易误报，后做）：

4. **对比度不足** — 取文字色和其背后实际背景色，算 WCAG 对比度，< 4.5 报警。
   难点是「背后实际背景色」要沿祖先链找第一个非透明背景，还要考虑渐变和图片。
5. **图片变形** — 渲染框宽高比 vs 原图宽高比，偏差超过阈值报警。
   原图尺寸现在已经存了（见 `84931c8`），可以直接比。

### 已经有的东西（不要重复造）

- `src/lib/xhs/measure-card.ts`
  - `findCardElement(doc)` — 从 `body` 往下穿过单子元素外壳，找到真正的卡片元素
  - `measureCard(frame)` — 读 iframe 里卡片的实际盒子
  - `CardBox` 类型
- iframe 是**同源**的（`sandbox="allow-scripts allow-same-origin"`），
  所以 `frame.contentDocument` 可以直接读，这是整套方案成立的前提。
- `src/components/xhs/step-cover.tsx` 的 `ScaledCover` 已经在这么干了，可以照抄取 frame 的方式。
- `src/components/xhs/step-render.tsx` 是成品预览，第 ④ 步的检查挂这里。

### 怎么算做对了

- 把上表五个案例的 HTML 做成测试夹具（前四个的原始 HTML 可以从对应提交的
  测试和 commit message 里复原），**修复前的版本必须被报出来，修复后的版本必须不报**。
- 误报是这个功能的主要风险：正常的卡片一条都不该报。
  拿 5 个内置模板的 `example.html` 当负样本跑一遍，应该全部干净。

### 界面

每张卡下面一行，点击跳到那一页，旁边给「只重生成这页」。
措辞要具体到数字，不要写「排版可能有问题」——写「654px 内容被裁掉」。

**它不判断好不好看。** 是烟雾报警器，不是设计评委。

---

## 2. 封面 prompt 里的模板示例太大（**待定，未决定是否要做**）

### 现状实测

一次封面生成是 3 个并发请求。三份 prompt 的差异：

```
每份长度   13,820 / 13,830 / 13,828
公共前缀   13,623   ← 99%
三份合计   41,478
```

**三次调用 99% 完全一样**，只有约 200 字符不同（视觉方向那句描述）。

⚠️ 先更正一个早先的错误判断：这**不是架构缺陷**。三次独立的 agent 调用本来就各自
需要完整上下文，重复是并行的固有成本，不是谁写错了。能动的只有两条路：
**少调用几次**，或**每次少发一点**。

### 路线 A：合并成一次调用出三版 —— 评估后不建议

省约 27,000 字符，但代价：

- 输出变成三份文档，长度翻三倍。**刚被输出截断坑过**（`f95f958`，图片 base64 抄到一半断掉）
- 失去「只重生成这一版」和「单版取消」——这两个是专门做的功能（三版里两版满意时不必全重来）
- 一版失败三版全废
- 流式结果要分别写进三个预览框，实现复杂度明显上升

省输入、赔可靠性和体验。不划算。

### 路线 B：把示例缩小（推荐先评估这条）

`deck-xhs-white/example.html` 的构成：

```
整份            8,432 字符
  <style> 样式   3,981   (47%)
  <body> 结构    4,167   (49%)   ← 7 张卡，每张约 595 字符
```

**封面只做一张卡，却收到了 7 张卡的结构**，其中 6 张是内容页版式，对封面是噪音。

改成「完整 `<style>` + 示例里的封面那一张卡」：

```
约 4,576 字符（现在 8,432）
单次省 3,856  →  一批三版省 11,568
```

已有先例：`trimExample()`（`src/lib/xhs/example-ref.ts`）在示例超出上限时就是保完整
`<style>`、丢结构，理由是「色板和组件类重新推导不出来，结构可以」。

第 ④ 步「出成品」要产出 N 张内容页，**保持发完整示例**，只改封面这一步。

### 实现要点

- 需要一个从示例 HTML 里切出第一张卡的函数。**正则做不了**（要配对嵌套的
  `<div>`），得数标签深度。是个小而可测的纯函数。
- 验收：5 个内置模板都能正确切出封面卡；切出来的片段单独渲染不报错；
  切完的示例仍然包含完整样式。

### 为什么不能简单删掉示例

它是修好「生成的和模板完全不像」的关键（`75ef6be`）。示例块占封面 prompt 的 **67%**、
成品 prompt 的 **53%**（`deck-xhs-white` + 11 页实测，砍规则之后；砍之前是 60% / 49%）。

### 顺带：prompt 缓存指望不上

prompt 顺序已经是最优形状（公共部分在前、变动的方向描述在倒数第 7 行）。但我们是
spawn 三个本地 CLI 进程并发跑的，**缓存归 CLI 和服务商管，设不了缓存断点**，而且三个
并发请求互相赛跑，第一个还没写完缓存后两个就发出去了。这条没什么可改的。

---

## 3. 强制引入 Tailwind CDN 是否还有必要

`XHS_CARD_DIRECTIVES`（`src/lib/xhs/prompts.ts`）里写着「在 `<head>` 中通过 CDN 引入
Tailwind v3 Play」。

- 最近几次生成**都加载了 Tailwind，却一个工具类都没用**，全是手写 CSS。
- `0110ad2` 那个「大标题消失」的 bug 正是因为 Tailwind 的 Preflight 重置了 `h1` 字号。
  现在有 `neutralizeTailwindPreflight()` 兜底，但这个麻烦本来可以不存在。
- 改成「按需引入」很可能更好，但**这是行为改变，必须真跑一次生成验证**，
  不能只靠改字面。

---

## 4. 清理

- `/api/convert`、`/api/draft` 两个接口**前端一次都没调用**，是从上游
  `html-anything` 继承的死代码。删之前确认 `src/lib/templates/shared.ts`
  （`assemblePrompt` / `SHARED_DESIGN_DIRECTIVES`）还有没有别的引用。
- 本地有个 `backup-zh-messages` 分支，是提交信息改英文之前的中文版历史备份。
  英文版已经稳定推送多轮，确认不需要了就可以删。

---

## 工作方式（这个项目踩过的坑）

1. **先判断是哪一层的问题**，再动手：
   - **代码层**（确定性、必然复现）→ 必须在代码里修，写进 prompt 是掩耳盗铃
   - **模板示例层**（`example.html` 会「教」agent）→ 改示例
   - **prompt 规则层**（概率性）→ 最后才用，而且要克制

2. **测量，不要看截图猜。** 这个项目里一半的初判是错的：
   「大标题消失」笃定是 prompt 让它缩字号，量完才发现字号从来没小过，是被覆盖的。

3. **每个修复配一个会变红的测试。** 把修复回退掉验证测试确实失败，否则测试只是摆设。

4. **加 prompt 规则要极其克制。** 已经发生过两次「新规则造成比它修的更严重的问题」
   （`3d9523b` 写得很清楚）。**每加一条规则，就多一个和其他规则打架的机会。**

5. 用户没有计算机背景。解释问题要给**可验证的数字**，不要堆术语。
